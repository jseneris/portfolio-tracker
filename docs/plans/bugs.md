# BUGS
Remove spinners from Sell allocation section
give error when over allocate shares 
splits still not working
speed issues
    time stamps/logs
simplify display lots

# Enhancements
backdate picture
more compare summary information
Show picture as of latest price in price history
    New transactions, get prices for open holdings instead of grabbing data range
    compare screen, check for hole on page load, only show button when missing transactions
    STock splits have to be explitly be activated for user, either on split screen or on stock history
    stock transaction table should always have original entry values
All cash amounts should be comma formatted
Dashboard now shows latest price, market value, and gain/loss amount.  Decide whether to remove cost basis.
Dashboard summary only needs available cash, current market value, performance
Display "as of date" on dashboard and stock history
Summary page, show year for each year has holdings descending

# Recent Changes
- Compare screen year options are now dynamic from transaction years plus an `All` option.
- Compare `All` view now uses one continuous timeline from earliest deposit through the capped end date.
- Compare portfolio valuation now carries forward the closest prior stock close on non-trading dates.
